# Fix and Merge PR #562 + Close All Open Work

## Current State

| Item | Status | Notes |
|------|--------|-------|
| **PR #562** | OPEN, BLOCKED | Tenant isolation (G16, G22). Cloudflare Pages FAIL + Codacy 10 issues (7 high, 3 medium) |
| **Issue #534** | CLOSED | G17 admin recovery — resolved by merged PR #563 |
| **PR #550** | OPEN | Dependabot chromaui/action bump. Pre-commit FAIL |
| **PR #551** | OPEN | Dependabot actions/labeler bump. Pre-commit + Labeler FAIL |

## Problem Analysis

### PR #562 Conflicts with Main

PR #562 (`fix/tenant-isolation-g16-g22-g18`) was created before PR #563 merged. Both touch:
- `apps/web/src/features/admin/BooksPage.tsx` (edit modal, archive button)
- `apps/web/src/i18n/en.ts`, `de.ts`, `fr.ts` (admin book i18n keys)
- `apps/worker/src/routes/admin/books.ts` (PATCH/DELETE endpoints)
- `packages/schema/src/schemas.ts` (UpdateBookSchema, etc.)

**PR #563 already shipped these changes.** PR #562's G18 portion is now redundant.

### What PR #562 Actually Adds (Unique to It)

The unique value is the **tenant isolation layer**:
- `apps/worker/src/lib/tenant-isolation.ts` — `parseLocatorRow()` + `assertBookAccess()`
- Route guards on `progress.ts`, `bookmarks.ts`, `highlights.ts`, `comments.ts`, `books.ts`
- `tenant-isolation.test.ts` tests
- Plans/docs updates (swarm analysis, ADRs, changelog)

### Cloudflare Pages Failure

Likely caused by the overlapping BooksPage.tsx changes conflicting with main's version after PR #563 merge.

### Codacy Issues (10 findings)

7 high + 3 medium — likely in:
- `tenant-isolation.ts` — async patterns, SQL string concatenation
- `comments.ts` — redundant grant queries
- `admin/books.ts` — SQL string concatenation in PATCH

## Execution Plan

### Wave 1: Rebase PR #562 onto main

1. Checkout `fix/tenant-isolation-g16-g22-g18` branch
2. Rebase onto latest main (which includes PR #563)
3. Resolve conflicts — keep only the unique tenant isolation code, drop the G18 overlap
4. Force-push the rebased branch

### Wave 2: Fix Codacy Issues

1. Review `tenant-isolation.ts` for async/await anti-patterns
2. Review `comments.ts` for redundant grant queries
3. Review `admin/books.ts` for SQL string concatenation concerns
4. Fix all findings and push

### Wave 3: Verify CI

1. Wait for Cloudflare Pages deploy to pass
2. Wait for Codacy to re-scan (should pass now)
3. Verify all other checks pass

### Wave 4: Merge PR #562

1. Add approval (self-merge with admin)
2. Squash merge
3. Delete feature branch

### Wave 5: Merge Dependabot PRs

1. Fix PR #550 pre-commit hook (likely commit message format)
2. Fix PR #551 pre-commit hook + labeler config compatibility
3. Merge both

## Files to Modify

| File | Action | Wave |
|------|--------|------|
| `apps/worker/src/lib/tenant-isolation.ts` | Fix Codacy issues | 2 |
| `apps/worker/src/routes/comments.ts` | Fix Codacy issues | 2 |
| `apps/worker/src/routes/admin/books.ts` | Fix Codacy issues | 2 |
| `apps/worker/src/__tests__/tenant-isolation.test.ts` | Verify tests pass | 3 |

## Verification

```bash
# After rebase:
./scripts/quality_gate.sh

# After Codacy fixes:
./scripts/quality_gate.sh
gh pr checks 562  # All green

# After merge:
gh pr list --state open  # Should be empty (or dependabot only)
gh issue list --state open  # Should be empty
```
