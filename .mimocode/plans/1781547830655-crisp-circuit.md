# Plan: Implement Remaining Tasks from plans/

**Date:** 2026-06-15
**Status:** Ready for implementation

## Context

After reviewing all 100+ plans and verifying against the actual codebase, the following items are genuinely incomplete. Swarm gaps G1–G28 and rate limiter DO are all done. The remaining work falls into three categories: CI fix, documentation gaps, and deferred feature work.

## Task 1: Fix CI lockfile mismatch (Issues #574, #575) — P0

Dependabot PRs #570 (js-yaml) and #571 (vite) bumped `apps/web/package.json` without updating `pnpm-lock.yaml`. CI runs `pnpm install --frozen-lockfile` which fails.

**Action:** `pnpm install` → commit updated lockfile → PR → close issues.

**Files:** `pnpm-lock.yaml`

## Task 2: Complete CHANGELOG.md entries — P1

Plan 084 specified adding entries for PRs #525, #527 and the swarm PRs. Currently CHANGELOG.md is missing entries for PRs #547–#558 (the main swarm PRs: Comments IDOR fix, Zod centralization, catalog route test, admin grants UI, compensating-controls test, reader panel mutual exclusivity).

**Action:** Add `[Unreleased]` bullets for PRs #547, #548, #549, #562, #563, #566, #568.

**Files:** `CHANGELOG.md`

## Task 3: Update ADR-INDEX.md with 11 missing ADRs — P1

11 ADR files exist on disk but aren't listed in `plans/ADR-INDEX.md`: ADR-065, ADR-067, ADR-072, ADR-073, ADR-074, ADR-077, ADR-079, ADR-081 (preexisting), ADR-082 (editorial), ADR-084, ADR-087.

**Action:** Add entries to ADR-INDEX.md with file path and one-line summary.

**Files:** `plans/ADR-INDEX.md`

## Task 4: Update AGENTS.md — remove stale rate-limiter reference — P2

AGENTS.md says "rate limiter DO cutover (Sprint 141 carry-over)" but this is already implemented (`rate-limiter-do.ts`, `rate-limit-client.ts`).

**Action:** Update AGENTS.md current phase section to reflect completion.

**Files:** `AGENTS.md`

## Task 5: Wire per-job CI path-aware skipping — P2

Plan 065 noted that `dorny/paths-filter` infrastructure exists in `ci.yml` but downstream jobs don't use the filter outputs to conditionally skip. The `changes` job runs but most jobs just depend on `setup` unconditionally.

**Action:** Add `if:` conditions to Typecheck, Lint, Unit Tests, Build, E2E Smoke, and Benchmark jobs based on `changes` outputs.

**Files:** `.github/workflows/ci.yml`

## Deferred (not in scope for this pass)

These are larger feature items from Plan 082 (Modern UI Overhaul) that are substantial new features, not gap closures:

- Library page (personal book collection vs catalog)
- Command Palette / search UI
- Custom Toast system
- Keyboard shortcuts
- Reusable EmptyState component
- `startTransition` for chapter nav
- Playwright FMP performance test

These should be tracked as new feature requests, not "missing tasks."

## Verification

1. `pnpm install --frozen-lockfile` — must succeed
2. `./scripts/quality_gate.sh` — must pass
3. CI on the lockfile PR must be green → close #574, #575
4. `plans/ADR-INDEX.md` — all ADR files on disk have an entry
5. `CHANGELOG.md` — all merged PRs since last entry are recorded
