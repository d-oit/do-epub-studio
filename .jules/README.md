# .jules/

Jules (google-labs-jules) specific configuration for `do-epub-studio`.

See `../../AGENTS.md` and `../../GEMINI.md` for the canonical rules.
This directory is intentionally thin: only Jules-specific overrides go
here (e.g. CI integration notes, branch-naming conventions).
