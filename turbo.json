{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["TURBO_TOKEN", "TURBO_TEAM"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["VITE_API_BASE_URL", "VITE_APP_NAME", "ANALYZE", "NODE_ENV"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "inputs": ["src/**", "tests/**", "*.config.*", "eslint.config.js"],
      "outputs": []
    },
    "typecheck": {
      "inputs": ["src/**", "tests/**", "tsconfig*.json"],
      "outputs": []
    },
    "test:unit": {
      "outputs": [],
      "inputs": ["src/**", "tests/**", "*.config.*", "vitest.workspace.ts", "vitest.config.ts"]
    },
    "test": {
      "dependsOn": ["test:unit"]
    },
    "test:e2e": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test:coverage": {
      "dependsOn": ["test:unit"],
      "outputs": []
    }
  },
  "globalDependencies": ["vitest.workspace.ts", "tsconfig.base.json"]
}
