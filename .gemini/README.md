# .gemini/

Gemini-specific configuration directory for `do-epub-studio`.

See `../../GEMINI.md` for the canonical adapter. See `../../AGENTS.md`
for the canonical rules every agent must follow.

This directory is intentionally thin: only provider-specific overrides go
here. Anything shared with other agents belongs in `AGENTS.md`.
