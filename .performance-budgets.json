{
  "gzipBudgets": {
    "mainJs": 180,
    "mainCss": 30,
    "lazyChunkJs": 80,
    "description": "Gzipped (zlib level 9) budgets in KB enforced by scripts/check-bundle-budget.mjs. Values aligned with ADR-107 §3 (180/30/80 KB). Supersedes per-file raw-byte bundleSize model removed in T4.1."
  },
  "routeBudgets": {
    "reader": {
      "entry": "src/features/reader/ReaderPage.tsx",
      "maxSize": 1190000,
      "description": "Total transitive JS and CSS for the reader route"
    },
    "catalog": {
      "entry": "src/features/catalog/CatalogPage.tsx",
      "maxSize": 895000,
      "description": "Total transitive JS and CSS for the catalog route (includes shared vendors)"
    },
    "admin": {
      "entry": "src/features/admin/AdminDashboardPage.tsx",
      "maxSize": 890000,
      "description": "Total transitive JS and CSS for the admin route (includes shared vendors)"
    }
  },
  "boundaryRules": {
    "catalog": {
      "entry": "src/features/catalog/CatalogPage.tsx",
      "forbiddenImports": ["reader-core"],
      "description": "Catalog shell must not import reader-core"
    },
    "auth": {
      "entry": "src/features/auth/LoginPage.tsx",
      "forbiddenImports": ["reader-core"],
      "description": "Auth shell must not import reader-core"
    },
    "admin": {
      "entry": "src/features/admin/AdminDashboardPage.tsx",
      "forbiddenImports": ["reader-core"],
      "description": "Admin shell must not import reader-core"
    }
  },
  "startupTime": {
    "fcp": 1500,
    "reader-fcp": 1200,
    "chapter-switch": 300,
    "offline-rehydrate": 800
  },
  "ciDuration": {
    "total": 900
  }
}
