name: Visual Regression
run-name: Visual Regression - ${{ github.event.pull_request.title || github.event_name }}

on:
  pull_request:
    branches: [main]
    paths:
      - 'packages/ui/**'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  chromatic:
    name: Chromatic visual regression
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: ./.github/actions/setup-pnpm

      - name: Build Storybook
        working-directory: ./packages/ui
        run: pnpm build:storybook --stats-json --output-dir storybook-static

      - name: Publish to Chromatic
        uses: chromaui/action@d92ea1ce501f70e8c34745b2c7888648150a368a  # v17.2.0
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          storybookBuildDir: storybook-static
          workingDir: packages/ui
          onlyChanged: true
          exitOnceUploaded: true
          exitZeroOnChanges: true
